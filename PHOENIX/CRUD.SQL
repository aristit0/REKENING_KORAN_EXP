CREATE TABLE REKENING_KORAN.CRUD (
JOB_ID BIGINT NOT NULL,
NIP BIGINT NOT NULL,
ACCTNO BIGINT ,
START_DATE INTEGER ,
END_DATE INTEGER,
JOB_STATUS VARCHAR,
REQ_DATE INTEGER,
LINK_DOWNLOAD VARCHAR,
TYPE VARCHAR(3),
CONSTRAINT PK_J PRIMARY KEY (JOB_ID DESC, NIP ) )
compression=SNAPPY,
salt_buckets=3;


CREATE SEQUENCE CRUD_SEQ MINVALUE 1 MAXVALUE 9223372036854775807 CYCLE;


UPSERT VALUES INTO REKENING_KORAN.CRUD  (JOB_ID, NIP, ACCTNO, START_DATE,END_DATE,JOB_STATUS, REQ_DATE) 
VALUES (NEXT VALUE FOR CRUD_SEQ, VALUE FOR ACCTNO, START_DATE YYYYMMDD, END_DATE YYYYMMDD, JOB_STATUS SUBMITTED, REQ_DATE NOW DATE WITH FORMAT YYYYMMDD)

UPSERT VALUES INTO my_table(id, col1, col2) 
VALUES( NEXT VALUE FOR my_schema.my_sequence, 'foo', 'bar');

